import express from 'express'
// import {fetchItems,getItem, insertItem, deleteItem, updateItem,addToCart} from '../controller/viewItemsController.js'
// import { verifyAToken } from '../middleware/authMiddleware.js'
// import { addToCartDB } from '../model/itemsDB.js'

const router = express.Router()

// router.post('/cart',verifyAToken, addToCart)

// router.get('/',verifyAToken, fetchItems)
// router.get('/:id',getItem)
// router.post('/insert',insertItem)
// router.delete('/:id', deleteItem)
// router.patch('/:id', updateItem)


export default router